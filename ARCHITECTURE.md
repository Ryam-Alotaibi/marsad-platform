<div dir="rtl">

# البنية المعمارية — مرصاد

هذه الوثيقة تُستكمل مع كل مرحلة من مراحل التنفيذ. المراحل 1-7 غطّت كل شاشات
القسم 5 (5.1 حتى 5.13) والطبقات المتقدمة الثلاث (6.1 مرصاد الاتحادي، 6.2 التوأم
الرقمي التوليدي، 6.3 استمرارية الخدمة) — خطة التنفيذ الأصلية المكوَّنة من 7
مراحل مكتملة بالكامل. المرحلة 8 (الحالية) طبقة تصليب وتشغيل حقيقية فوق ذلك:
عزل جهات حقيقي على مستوى قاعدة البيانات (Row-Level Security)، مصادقة بكوكيز
httpOnly بدل localStorage، تحديد معدل الطلبات، سجل تدقيق حقيقي، مزوّدات
تكامل حقيقية (Twilio/SMTP/Anthropic) مفعَّلة بشرط توفر بيانات اعتماد حقيقية،
معالج إعداد جهة جديدة حقيقي، وخط CI. راجع الأقسام أدناه لكل واحدة بالتفصيل.

## نظرة عامة على البنية

```
┌─────────────┐      REST/JSON       ┌──────────────┐      Prisma      ┌────────────┐
│  apps/web   │ ───────────────────▶ │   apps/api   │ ────────────────▶│ PostgreSQL │
│  (Next.js)  │ ◀─────────────────── │   (NestJS)   │ ◀─────────────────│            │
└─────────────┘        JWT           └──────────────┘                  └────────────┘
                                             │
                                             │ POST /federated/train-and-aggregate
                                             ▼
                                      ┌──────────────┐
                                      │   apps/ml    │
                                      │  (FastAPI)   │
                                      └──────────────┘
```

- **apps/web**: Next.js App Router، كل طلب يُصادَق بكوكي httpOnly (`marsad_session`)
  يُرسَل تلقائيًا عبر `credentials: "include"` — لا يوجد أي توكن مخزَّن بـ localStorage
  أو قابل للقراءة من JavaScript. راجع قسم "المصادقة والأدوار" أدناه.
- **apps/api**: نقطة الدخول الوحيدة لقاعدة البيانات. كل موديول يعتمد على `PrismaService`
  (مسجَّل كـ Global Module) بدل الاتصال المباشر.
- **apps/ml**: خدمة `/health`، وخدمة تدريب/تجميع مرصاد الاتحادي
  (`/federated/train-and-aggregate`) بخوارزمية Federated Averaging حقيقية.
  محرك التوأم الرقمي التوليدي (محاكاة السيناريوهات، `apps/api/src/scenarios`)
  يستهلك أوزان النموذج العام الناتج عن هذا التدريب مباشرة (`FLModel.globalWeightsRef`).

## تعدد الجهات (Multi-Tenancy)

- كل جدول ذو صلة ببيانات جهة يحمل `tenantId` مباشرة، أو يصل إليه عبر
  `siteId → Region → Tenant`.
- العزل مُفروض على **طبقتين مستقلتين حقيقيًا** الآن (راجع القسم التالي لتفاصيل RLS):
  1. طبقة التطبيق: كل موديول يمرّر `tenantId` (من الجلسة) ضمن شرط `WHERE`.
  2. طبقة قاعدة البيانات: Row-Level Security فعلي على PostgreSQL — حتى لو نُسي شرط
     `WHERE` بموديول مستقبلي، القاعدة نفسها ترفض إرجاع صفوف جهة أخرى.
- ألوان وهوية كل جهة (`primaryColor`, `secondaryColor`, `logoUrl`) تُحقَن كمتغيرات CSS
  (`--brand-primary`, `--brand-secondary`) على مستوى صفحة لوحة التحكم، فتُطبَّق تلقائيًا
  على أي مكوّن يستخدم `bg-brand` / `text-brand` بدون كود خاص بكل جهة.

## عزل الجهات على مستوى قاعدة البيانات (Row-Level Security)

- **تفعيل فعلي وليس توثيقًا فقط**: هجرة `enable_row_level_security` تُفعِّل RLS
  و`FORCE ROW LEVEL SECURITY` (يُطبَّق حتى على الدور المالك للجداول) على 16 جدولًا
  تحمل `tenantId` مباشرة ولا تُقرَأ/تُكتَب عبر حدود الجهات ضمن طلب واحد شرعيًا:
  `alerts`, `chat_conversations`, `device_categories`, `escalation_rules`,
  `notification_channels`, `playbooks`, `predictions`, `rationing_schedules`,
  `risk_factors`, `scenario_runs`, `scheduled_services`, `system_components`,
  `telecom_providers`, `audit_logs`, `regions`. السياسة: `"tenantId" =
  current_setting('app.current_tenant_id', true)`.
  - **استُثنيت عمدًا** (مع التوثيق داخل ملفات الهجرة نفسها): `User`/`Role`/`Tenant`
    (تُستعلَم قبل تسجيل الدخول، أو هي الجذر نفسه)؛ `FLParticipation`/`FLTenantUpdate`
    (منسِّق التعلّم الاتحادي يقرأ/يكتب عبر كل الجهات المشارِكة عمدًا ضمن طلب واحد —
    هذا هو جوهر الفكرة)؛ `sites` (اكتُشف أثناء التحقق أن منسِّق التعلّم الاتحادي يقرأ
    PowerReading/WeatherReading لكل جهة مشارِكة عبر `site: { tenantId }` — RLS على
    `sites` كانت تُخفي مواقع الجهات الأخرى بصمت وتُسقط جولة تدريب متعددة الجهات إلى
    جهة واحدة فقط، فاكتُشف هذا فعليًا بتشغيل جولة حقيقية بـ3 جهات قبل الإصلاح، وليس
    نظريًا).
- **آلية التفعيل بالطلب**: `apps/api/src/prisma/tenant-context.interceptor.ts` يفتح
  معاملة (transaction) واحدة لكل طلب مُصادَق، وينفّذ
  `SELECT set_config('app.current_tenant_id', $tenantId, true)` (نطاقها محصور
  بالمعاملة نفسها، `true` = `is_local`)، ثم يمرّر عميل تلك المعاملة عبر
  `AsyncLocalStorage` (`tenant-context.ts`). `PrismaService` (`prisma.service.ts`)
  لم يعد يرث `PrismaClient` مباشرة، بل يُغلِّفه بـ Proxy يحوِّل كل استدعاء موجود
  (`this.prisma.alert.findMany(...)`) تلقائيًا لعميل المعاملة المحصورة بالجهة دون
  تعديل أي موديول من الموديولات العشرين الموجودة مسبقًا.
- **فصل أدوار قاعدة البيانات**: دور تشغيل الـ API (`marsad` محليًا، `marsad_app` بـ
  docker-compose) عادي وليس Superuser ولا BYPASSRLS. الهجرات والزرع (`prisma
  migrate`, `prisma db seed`) تتصل بدور مرتفع الصلاحية منفصل عبر `MIGRATOR_DATABASE_URL`
  (`directUrl` بـ `schema.prisma`) — بدون هذا الفصل، لو كان دور التطبيق نفسه هو
  الذي يُشغّل الهجرات (Superuser بحكم إنشاء قاعدة البيانات)، لكان يتجاوز RLS تلقائيًا
  بصرف النظر عن أي سياسة، ويُفرِغ الحماية من قيمتها تمامًا. `docker-compose.yml`
  يطبّق نفس الفصل عبر `infra/postgres-init/01-create-app-role.sql` (يُنشئ دور
  `marsad_app` عند أول تشغيل ويمنحه صلاحيات محدودة فقط).
- **التحقق الفعلي**: تأكّدنا يدويًا عبر `psql` أن دور `marsad` بدون سياق جهة يرى
  0 صف من جدول `sites`، بينما الدور المرتفع يرى الكل؛ وأن تعيين السياق لجهة معيّنة
  يُظهر فقط صفوف تلك الجهة حتى مع استعلام صريح يطلب صفوف جهة أخرى. اختبرنا أيضًا
  تدفقًا كاملًا عبر التطبيق (تسجيل دخول جهتين مختلفتين، كل واحدة ترى بياناتها فقط)
  ومسار كتابة (إنشاء تنبيه) للتأكد من أن `WITH CHECK` لا يرفض الكتابات الشرعية.

## المصادقة والأدوار

- تسجيل الدخول (`POST /auth/login`) يتحقق من البريد وكلمة المرور (bcrypt)، ويصدر JWT
  يحمل `sub` (معرّف المستخدم)، `tenantId`، و`roleKey` — **لا يُعاد بجسم الاستجابة
  إطلاقًا**، بل يُخزَّن مباشرة كقيمة كوكي httpOnly (`marsad_session`, `SameSite=Lax`,
  `Secure` بالإنتاج) عبر `res.cookie(...)` بـ `auth.controller.ts`. الجسم المُعاد
  يحوي فقط بيانات الجلسة (المستخدم + الجهة) لعرضها بالواجهة.
- `JwtStrategy` (`apps/api/src/auth/strategies/jwt.strategy.ts`) يستخرج التوكن من
  الكوكي حصرًا (`ExtractJwt.fromExtractors([fromCookie])`) — لا يوجد قبول لـ
  `Authorization: Bearer` بعد الآن.
- `POST /auth/logout` يمسح الكوكي فعليًا من طرف الخادم (`res.clearCookie`) — الفرونت
  إند لا يمكنه حذف كوكي httpOnly بنفسه.
- **الأثر الأمني الحقيقي**: تحقّقنا عبر المتصفح أن `document.cookie` يُعيد سلسلة فارغة
  وأن `localStorage` لا يحوي أي توكن بعد تسجيل الدخول — أي ثغرة XSS مستقبلية لن تستطيع
  سرقة الجلسة عبر قراءة تخزين الواجهة، بخلاف النمط السابق (`localStorage.setItem`).
- `GET /auth/me` يُستخدم لاسترجاع بيانات الجلسة (المستخدم + الجهة) عند تحميل الواجهة.
- تحديد معدل الطلبات (`@nestjs/throttler`) مفعَّل عالميًا (120 طلب/دقيقة) مع حد أشد
  خصوصًا على `POST /auth/login` (8 محاولات/دقيقة) و`POST /tenants/setup`
  (5/دقيقة) لمقاومة محاولات تخمين كلمات المرور والاستخدام المُفرط لمعالج الإعداد.
- `RolesGuard` + `@Roles(...)` (بـ `apps/api/src/auth/guards`) — أول استخدام فعلي
  لهما بهذا المستودع هو تقييد `GET /audit-logs` لمديري الجهة/النظام فقط (راجع قسم
  سجل التدقيق أدناه).
- الأدوار السبعة الافتراضية مصدرها الوحيد `packages/shared/src/roles.ts` — تُستخدم
  بالواجهة (لعرض المسمى بالعربي) وبسكربت الزرع (لإنشاء صفوف `Role` لكل جهة) لتفادي أي
  تكرار أو تعارض بالتسمية.

## قاعدة البيانات ↔ الشاشات

مخطط Prisma الكامل بـ [apps/api/prisma/schema.prisma](apps/api/prisma/schema.prisma).
الجدول التالي يربط كل مجموعة موديولات بالشاشة أو الوحدة التي تخدمها بخطة المنصة، لإثبات
أن التصميم متوافق مع كل الشاشات المطلوبة قبل بناء واجهاتها:

| مجموعة الموديولات | الشاشة/الوحدة المرتبطة | حالة الواجهة |
|---|---|---|
| `Tenant`, `Region`, `Site`, `Role`, `User` | تعدد الجهات، معالج الإعداد، RBAC | ✅ مبنية جزئيًا (تسجيل الدخول + لوحة أساسية) |
| `SystemComponent` | 5.4 لوحة التحكم التفصيلية | ✅ مبنية (`/dashboard/systems`) |
| `Sensor`, `SensorReading` | 5.10 مستشعرات IoT | ✅ مبنية (`/dashboard/sensors` — 6 أنواع مستشعرات، تصنيف حرج/تحذير/طبيعي، خريطة) |
| `PowerReading` | 5.5 خريطة الكهرباء | ✅ مبنية (`/maps/power` + منحنى الحمل) |
| `TelecomProvider`, `TelecomStatusReading` | 5.5 خريطة الاتصالات | ✅ مبنية (`/maps/telecom` + مصفوفة NOC) |
| `Site.infrastructureAgeYears`, `RiskFactor`, `Alert` (عدد) | 5.5 خريطة المخاطر | ✅ مبنية (`/maps/risk`) |
| `WeatherReading` | 5.5 خريطة الطقس | ✅ مبنية (`/maps/weather`) |
| `WeatherReading`, `PowerReading`, `TelecomStatusReading`, `ScheduledEvent` | 5.6 المخاطر البيئية | ✅ مبنية (`/environment`) |
| `Prediction`, `PredictionAction` | 5.2 التنبؤ الذكي العاجل | ✅ مبنية (`/dashboard/predictions`) |
| `RiskFactor` | 5.3 التنبيهات والمخاطر الموحد | ✅ مبنية (`/dashboard/alerts`) |
| `Alert`, `EscalationRule`, `EscalationLog`, `User.availabilityStatus` | 5.7 تنبيهات الدعم الفني ومصفوفة التصعيد | ✅ مبنية (`/dashboard/support` — توجيه تلقائي لأقرب عضو متاح) |
| `NotificationChannel`, `NotificationLog` | 5.8 محرك الإشعارات متعدد القنوات | ✅ مبنية (معاينة وإرسال حقيقيان لـ5 قنوات، مزود Mock جاهز للاستبدال) |
| `AlertPreference` | 5.11 تنبيهات مخصصة | ✅ مبنية (`/dashboard/preferences`) |
| `DeviceCategory`, `RationingSchedule`, `EnergyConsumptionLog` | 5.9 ترشيد الطاقة | ✅ مبنية (`/dashboard/energy` + إجراءات حقيقية) |
| `ChatConversation`, `ChatMessage` | 5.12 المستشار الذكي | ✅ مبنية (`/dashboard/advisor` — استرجاع حقيقي من قاعدة البيانات، توليد Mock قابل للاستبدال بـ LLM حقيقي) |
| — (محتوى ثابت بالواجهة) | 5.13 دليل المستخدم | ✅ مبنية (`/dashboard/guide` — مقدمة تتغيّر حسب نوع الجهة) |
| `FLModel`, `FLRound`, `FLTenantUpdate`, `FLParticipation` | 6.1 مرصاد الاتحادي | ✅ مبنية (`/dashboard/federated` — FedAvg حقيقي، سجل تدقيق مشفّر) |
| `ScenarioRun`, `ScenarioImpact`, `Playbook` | 6.2 التوأم الرقمي التوليدي | ✅ مبنية (`/dashboard/future` — What-If يدوي + مسح ليلي عشوائي + تجميد/تفعيل خطط طوارئ) |
| `ScheduledService`, `ContinuityAction` | 6.3 محرك استمرارية الخدمة | ✅ مبنية (`/dashboard/continuity` — تقييم وتنفيذ إجراءات استمرارية حقيقية) |
| `AuditLog` | سجلات تدقيق عامة (7. متطلبات غير وظيفية) | ✅ مبنية (`/dashboard/audit-log` — مديرو الجهة/النظام فقط) |

## مرصاد الاتحادي (6.1) — كيف يعمل فعليًا

- **البيانات لا تغادر الجهة أبدًا**: `apps/api` يجمع خصائص تدريب حقيقية من قراءات
  كل جهة (الحمل الكهربائي%، الحرارة، الرطوبة، AQI عند كل موقع/توقيت، ~20 عيّنة لكل
  جهة) والتسمية (label) دالّة معروفة (وليست بيانات مُلفَّقة) تمزج الحمل والطقس —
  ثم يرسل هذه العيّنات فقط إلى `apps/ml` **الخاص بنفس الخادم**، وليس عبر الشبكة بين
  جهات مختلفة فعليًا؛ التصميم مع ذلك يُحاكي بدقّة الحدود التي سيفرضها نشر حقيقي
  متعدد الخوادم (كل ما يُرسَل هو ميزات مُشتقّة رقمية، لا سجلات خام).
- **التدريب المحلي**: لكل جهة، `apps/ml` يدرّب انحدارًا خطيًا حقيقيًا (Least Squares
  عبر numpy) على عيّنات تلك الجهة فقط (`app/federated.py: fit_linear_model`).
- **Federated Averaging حقيقي**: `app/federated.py: fedavg` — متوسط مرجّح لأوزان كل
  النماذج المحلية حسب حجم بيانات كل جهة (خوارزمية FedAvg الموثّقة، McMahan et al.
  2017)، مُختبَرة بوحدات اختبار صريحة (`apps/ml/tests/test_federated.py`، 10
  اختبارات تُحقق صحة الحساب الرياضي، وليس مجرد المحاكاة البصرية).
- **قياس التحسّن فعليًا**: لكل جهة تُقاس الدقة (100 − MAE% على عيّنات تحقّق لم
  تُستخدم بالتدريب) قبل التجميع (نموذجها المحلي فقط) وبعده (النموذج العام) —
  الفرق مرئي بلوحة `/dashboard/federated`.
- **سجل تدقيق مشفّر حقيقي**: كل تحديث جهة يُخزَّن بـ `FLTenantUpdate` مع
  `auditHashSelf = sha256(tenantId + roundId + weightsHash + auditHashPrev + timestamp)`
  — سلسلة تجزئة متسلسلة فعليًا (كل تجزئة تعتمد على سابقتها)، تُثبت شرعية كل
  تحديث دون كشف بيانات الجهة الخام.
- **قابلية التوسع**: `POST /federated/rounds/run` مستقل عن أي جهة معيّنة، ويستقبل
  بيانات أي عدد من الجهات المشارِكة — إضافة جهة خارجية حقيقية لاحقًا تتطلب فقط أن
  ترسل ميزاتها المحلية بنفس الصيغة، دون تعديل خوارزمية التجميع.

## التوأم الرقمي التوليدي (6.2) — كيف يعمل فعليًا

- **نفس النموذج الحقيقي، لا محاكاة بصرية منفصلة**: `apps/api/src/scenarios/scenarios.service.ts`
  يقرأ `FLModel.globalWeightsRef` (الأوزان المُدرَّبة فعليًا بمرصاد الاتحادي 6.1 —
  انحدار خطي حقيقي على `loadPct`, `temperatureC`, `humidityPct`, `aqi`) ويطبّقها
  مباشرة لحساب درجة الخطورة الأساسية لكل سيناريو. إن لم يكتمل أي جولة تدريب بعد،
  يُستخدم بديل احتياطي مُوثَّق صراحةً بالكود بدل الفشل الصامت.
- **تمايز حقيقي بين المواقع**: التأثير المتتالي (`ScenarioImpact`) لكل موقع يُحسَب
  بضرب الخطورة الأساسية بمُعدِّلات فعلية من بيانات ذلك الموقع تحديدًا — عمر البنية
  التحتية (`Site.infrastructureAgeYears`)، عدد التنبيهات المفتوحة، ومدة انقطاع
  الكهرباء/محاولات الدخول المشبوهة المُدخَلة بالسيناريو — لا قيمة موحّدة لكل المواقع.
  تحقّقنا يدويًا من تفاوت فعلي (مثلًا 68.9% مقابل 62.1%) بين مواقع بنفس السيناريو.
- **سبب جذري نصّي وليس رقمًا فقط**: `composeRootCause()` يبني سردًا عربيًا شرطيًا —
  كل جملة بالتفسير مرتبطة فعليًا بعامل تجاوز عتبته بذلك السيناريو تحديدًا (لا نص
  ثابت يتكرر لكل النتائج).
- **اكتشاف "غير مسبوق" حقيقي**: `isNovel()` يقارن تركيبة العوامل (مُقرَّبة) مع كل
  السيناريوهات السابقة المخزَّنة؛ التركيبة الجديدة فعليًا تُعلَّم كذلك.
- **المسح الليلي مضبوط ليعكس ندرة الاكتشاف الحقيقي**: `randomFactors()` يستخدم توزيعًا
  مرجّحًا (80% مدى طبيعي / 20% مدى متطرف لكل عامل) بدل توزيع منتظم، مع عتبة تأثير
  `SWEEP_IMPACT_THRESHOLD = 90` — ضُبطت تجريبيًا (من ~23 اكتشافًا من كل 25 محاولة إلى
  ~7 من 25) لتفادي إغراق المستخدم بتنبيهات "اكتشاف جديد" لا معنى لها.
- **خطط الطوارئ**: أي نتيجة محاكاة يمكن تجميدها (`freezePlaybook`) باسم مخصّص، ثم
  تفعيلها لاحقًا (`activatePlaybook`) — تحقّقنا من كلا المسارين فعليًا عبر المتصفح
  ومراقبة طلبات API (`POST /scenarios/:id/freeze`, `POST /scenarios/playbooks/:id/activate`
  يعيدان 201 Created).

## محرك استمرارية الخدمة (6.3) — كيف يعمل فعليًا

- **مصدران حقيقيان فقط**: `apps/api/src/continuity/continuity.service.ts` يقبل إما
  تنبؤًا نشطًا حقيقيًا (`Prediction`، بما فيه نافذته الزمنية الفعلية `windowStart`/
  `windowEnd` ودرجة ثقته) أو خطة طوارئ مُفعَّلة من التوأم الرقمي (`Playbook` مُفعَّل،
  مبني على `ScenarioRun.impactScore` الفعلي ومواقعه المتأثرة `ScenarioImpact` الحقيقية)
  — لا مصدر ثالث وهمي.
- **فحص الخدمات المجدولة الفعلية عند المواقع المتأثرة فقط**: لكل موقع متأثر، يُستعلَم
  عن `ScheduledService` المجدولة فعليًا ضمن نافذة المصدر الزمنية (نافذة التنبؤ للتنبؤات،
  أو 72 ساعة قادمة للسيناريوهات الافتراضية) — لا فحص عشوائي لكل الخدمات.
  - إن كانت الخطورة أقل من 70% لا يُقترح أي إجراء إطلاقًا (تفاديًا لتعطيل خدمات لا
    تستدعي ذلك فعليًا).
- **قرار الإجراء منطق قواعد حقيقي وليس تخمينًا**: `decideAction()` يفرّق بين خطورة
  حرجة (90%+) ومرتفعة (70-89%)، ونوع الخدمة (جلسة رسمية تتطلب حضورًا فعليًا غالبًا،
  مقابل موعد/معاملة يمكن تنفيذها عن بُعد)، ووجود موقع بديل فعليًا ضمن نفس المنطقة
  (`findAlternateSite` يبحث عن الموقع الشقيق الأقل عددًا من التنبيهات المفتوحة) —
  لا يُقترح "إعادة توجيه" إن لم يوجد فعليًا موقع بديل صالح.
- **سبب نصّي مرتبط بالحالة الفعلية**: `composeReason()` يذكر درجة الخطورة الحقيقية
  والموقع والموقع البديل الفعلي (إن وُجد)، لا نصًا عامًا مكررًا.
- **إشعار المستفيد حقيقي المحتوى**: عند تنفيذ الإجراء (`POST /continuity/actions`) يُنشأ
  سجل `ContinuityAction` فعلي، ويُبنى نص إشعار عربي يذكر نوع الخدمة والموعد الأصلي
  والإجراء المتخذ تحديدًا — معروض للمستخدم كمعاينة (Mock provider، بنفس منهجية
  الشفافية المتبعة بمحرك الإشعارات 5.8، موثّق كذلك صراحة).
- **منع الازدواجية**: كل اقتراح يُفحَص مقابل `ContinuityAction` سابقة لنفس الخدمة
  ونفس المصدر (`alreadyApplied`)، فلا يُعاد اقتراح إجراء نُفِّذ فعلًا.
- تحقّقنا من المسار الكامل عبر المتصفح فعليًا: تقييم تنبؤ نشط حقيقي من بيانات Seed
  (خطورة 78%) → اقتراح "إعادة توجيه لموقع آخر" مع سبب مرتبط بالبيانات الفعلية →
  تنفيذ الإجراء (`POST /continuity/actions` يعيد 201 Created) → ظهوره فورًا في سجل
  الإجراءات مع كل التفاصيل الصحيحة.

## محرك الإشعارات متعدد القنوات (5.8) — كيف يعمل فعليًا

- `NotificationsService` يولّد نصًا مختلفًا لكل قناة (Push قصير، Email رسمي بعنوان
  وتحية، SMS محدود بـ160 حرفًا، WhatsApp حواري، ونص مكالمة آلية جاهز لتحويل نصي-صوتي)
  من نفس بيانات `Alert` — وليس نصًا واحدًا مكررًا.
- `GET /notifications/preview/:alertId` يُرجع المعاينة دون كتابة بقاعدة البيانات.
  `POST /notifications/send/:alertId` يكتب صفًا حقيقيًا بـ `NotificationLog` لكل قناة
  مفعّلة (`NotificationChannel.isActive`)، بمستلم مُحدَّد فعليًا (أقرب عضو دعم فني متاح).
- **مزوّدات حقيقية مشروطة بوجود بيانات اعتماد فعلية** (`apps/api/src/notifications/providers`):
  `TwilioProvider` (SMS حقيقي، WhatsApp عبر Twilio Sandbox/Business API، مكالمة صوتية
  بـ TwiML) و`SmtpProvider` (بريد حقيقي عبر `nodemailer`). كل مزوّد يتحقق من اكتمال
  متغيرات البيئة الخاصة به (`TWILIO_ACCOUNT_SID`+...، `SMTP_HOST`+...) عبر خاصية
  `isConfigured`؛ بدونها — وهو الوضع الافتراضي بهذا المستودع، إذ لا نملك حسابات
  Twilio/SMTP فعلية — `NotificationsService.dispatch()` يسقط تلقائيًا لسلوك Mock
  الموثَّق سابقًا (نجاح دائم، بلا اتصال شبكي فعلي). PUSH يبقى Mock دائمًا (يحتاج
  بيانات اعتماد FCM/APNs غير متوفرة).
  - **تمييز حقيقي وليس افتراضًا**: `NotificationLog` اكتسب عمودين جديدين — `isMock`
    (`false` فقط عند نجاح إرسال حقيقي فعليًا) و`providerRef` (مُعرِّف الرسالة/المكالمة
    الحقيقي من Twilio/SMTP، `null` بوضع Mock) — بدلًا من افتراض النجاح دون دليل.
  - تحقّقنا فعليًا عبر استدعاء `POST /notifications/send/:alertId` بدون بيانات اعتماد
    مهيَّأة: كل القنوات الخمس رجعت `isMock: true, providerRef: null, status: "SENT"`
    كما هو متوقع تمامًا — المسار البديل (fallback) يعمل بصمت دون كسر أي شيء.

## المستشار الذكي (5.12) — كيف يعمل فعليًا

- الاسترجاع حقيقي بالكامل: `ChatService` يكتشف نيّة السؤال (كهرباء/تنبؤات/تنبيهات/طقس/
  مستشعرات) بمطابقة كلمات مفتاحية عربية، ثم يستعلم فعليًا عن أحدث القراءات والتنبؤات
  والتنبيهات من قاعدة البيانات لتلك الجهة تحديدًا. **هذه الخطوة لا تتغيّر إطلاقًا** بصرف
  النظر عن توفّر نموذج لغوي حقيقي أم لا.
- **إعادة صياغة حقيقية اختيارية** (`apps/api/src/chat/providers/llm.provider.ts`):
  عند توفّر `OPENAI_API_KEY` يُستدعى OpenAI (`gpt-4o-mini`)، أو عند توفّر `ANTHROPIC_API_KEY`
  فقط يُستدعى Claude (`claude-haiku-4-5-20251001`) — OpenAI له الأولوية إن وُجد المفتاحان
  معًا. كلا المزوَّدين يُستدعى بنفس نص النظام الصريح الذي يُلزمه بالاعتماد "حصريًا على
  الحقائق المزوَّدة" (نص القالب المُركَّب من البيانات الحقيقية + JSON السياق) دون اختراع
  أي رقم جديد — دور النموذج إعادة الصياغة فقط، وليس توليد الإجابة من الصفر. بدون أي من
  المفتاحين — وهو الوضع الافتراضي بهذا المستودع — يُستخدم نص القالب مباشرة (نفس سلوك ما
  قبل هذه المرحلة، تحقّقنا منه فعليًا عبر استدعاء حقيقي).
- "التحديد المتعدد اللحظي" (`GET /chat/composite-alerts`) يفحص فعليًا تزامن أكثر من
  مؤشر خطر على نفس الموقع (حمل كهربائي مرتفع + حرارة مرتفعة، أو تنبيه حرج + درجة مخاطر
  مرتفعة) — وزر الإجراء بالواجهة يستدعي endpoints حقيقية موجودة مسبقًا (تفعيل الترشيد
  الليلي، إيقاف التكييف)، وليس زرًا شكليًا.

## سجل التدقيق (Audit Log)

- `AuditLogService.record()` (`apps/api/src/audit-log`) يُستدعى من نقاط تغيير حقيقية
  حسّاسة فقط — لا من كل قراءة: تسجيل الدخول، إنشاء تنبيه، إرسال إشعارات، تجميد/تفعيل
  خطة طوارئ، تنفيذ إجراء استمرارية، تشغيل جولة تعلّم اتحادي (سجل مستقل لكل جهة مشارِكة،
  مع تمييز الجهة التي فعليًا طلبت الجولة عبر `triggeredByThisTenant`).
- **يعمل بأمان من أي سياق**، بما فيه تسجيل الدخول نفسه (قبل وجود أي جلسة/سياق جهة):
  `record()` يفتح معاملة مستقلة خاصة به ويضبط `app.current_tenant_id` بنفسه صراحة،
  بدل الاعتماد على السياق المحيط بالطلب — لأن `audit_logs` نفسه محمي بـ RLS (راجع
  القسم أعلاه)، ولا سياق جهة متاح أثناء تسجيل الدخول تحديدًا.
- `GET /audit-logs` محمي بـ `RolesGuard` (`TENANT_ADMIN`, `SUPER_ADMIN` فقط) — أول
  استخدام فعلي لهذا الـ Guard المُجهَّز سابقًا دون استخدام. صفحة `/dashboard/audit-log`
  بالواجهة تُخفي رابطها بالقائمة الجانبية تلقائيًا عن غير المديرين (`user.roleKey`).
- تحقّقنا فعليًا: تسجيل دخول ثم إنشاء تنبيه حقيقي بالمتصفح، ثم مراجعة السجل — كلا
  الإجراءين ظهرا فورًا بالتفاصيل الصحيحة (اسم الفاعل، البيانات الوصفية الحقيقية،
  الطابع الزمني)، وليس بيانات تجريبية مُعدَّة مسبقًا.

## معالج إعداد جهة جديدة (Tenant Setup Wizard)

- `POST /tenants/setup` (`apps/api/src/tenants`) يستبدل ما كان "endpoint + سكربت زرع
  فقط" بمسار onboarding ذاتي حقيقي: يُنشئ الجهة، أدوارها السبعة الافتراضية (نفس
  `packages/shared/src/roles.ts` المصدر الوحيد المستخدم بسكربت الزرع)، منطقة وموقع
  أوَّلَين (حتى لا تكون لوحات الجهة الجديدة فارغة كليًا)، وحساب `TENANT_ADMIN` الأول
  (bcrypt) — كل ذلك بمعاملة واحدة ذرّية.
  - **تعارض حقيقي مع RLS اكتُشف ووُثِّق أثناء التحقق**: إنشاء `Region`/`Site` (محميان
    بـ RLS) داخل معاملة onboarding عامة (بلا سياق جهة بعد — الجهة نفسها تُنشأ للتو
    بنفس المعاملة) كان يفشل بخطأ RLS حقيقي (`42501`) عند أول تشغيل فعلي عبر الواجهة،
    وليس افتراضًا نظريًا. الإصلاح: `SELECT set_config('app.current_tenant_id',
    $newTenantId, true)` صراحة داخل نفس المعاملة فور إنشاء الجهة، قبل إنشاء
    Region/Site — شرعي لأن كل ما يُنشأ بعدها ضمن نفس المعاملة ينتمي فعلًا لتلك الجهة.
- الواجهة (`/setup`، صفحة عامة خارج `SessionProvider`) معالج من 4 خطوات حقيقي (بيانات
  الجهة، الموقع الأول، حساب المدير، مراجعة) بحالة نموذج واحدة وتحقّق تدريجي — وليس
  نموذجًا واحدًا طويلًا. `POST /tenants/setup` محدود بـ 5 محاولات/دقيقة.
- تحقّقنا من المسار كاملًا بالمتصفح: تعبئة الخطوات الأربع → إنشاء ناجح → تسجيل دخول
  فوري بالحساب الجديد → لوحة تحكم تعرض هوية/ألوان الجهة الجديدة بشكل صحيح وحالة فارغة
  صادقة (بلا بيانات وهمية) لأنه لا توجد بيانات تشغيلية بعد لجهة حديثة الإنشاء.

## التكامل المستمر ودعم الحاويات (CI / Docker)

- `.github/workflows/ci.yml`: مهمتان مستقلتان — بناء (`packages/shared` ثم `apps/web`
  ثم `apps/api`، بما فيها `prisma generate`) واختبارات `apps/ml` (`pytest`، الاختبارات
  الحقيقية العشرة لخوارزمية FedAvg من المرحلة 5). كلا الأمرين تحقَّقنا من نجاحهما محليًا
  بنفس الأوامر بالضبط (`npm run build` من الجذر، و`pytest` داخل بيئة Python نظيفة) قبل
  كتابة الـ workflow، وليس تخمينًا لصيغة الأوامر.
  - اكتُشف فعليًا أثناء هذا التحقق أن `pytest` بمجلد نظيف لا يجد وحدة `app` (لا يوجد
    `sys.path` صريح) — أُضيف `apps/ml/pytest.ini` (`pythonpath = .`) لإصلاحه، بعد
    التأكد من إعادة الاختبار بنجاح.
- `docker-compose.yml`: يُنشئ حاوية Postgres واحدة (`POSTGRES_USER` بها Superuser
  بحكم آلية `initdb` الرسمية)، وحاويات `api`/`web`/`ml`. **لتفادي أن يكون دور تشغيل
  الـ API نفسه Superuser** (ما يُبطل RLS تمامًا بصمت)، `infra/postgres-init/01-create-app-role.sql`
  يُنشئ دور `marsad_app` محدود الصلاحية عند أول تشغيل عبر آلية `docker-entrypoint-initdb.d`
  الرسمية، وتُمنَح صلاحياته تلقائيًا لأي جدول تُنشئه الهجرات لاحقًا عبر
  `ALTER DEFAULT PRIVILEGES`. حاوية `api` تتصل بـ`marsad_app` للتشغيل العادي، وبـ
  `POSTGRES_USER` (المرتفع) فقط لـ `MIGRATOR_DATABASE_URL` عند الترحيل.
  - **حدود التحقق المُقِرّ بها صراحة**: لا يوجد Docker مثبَّت بهذه البيئة المحلية،
    فتحقّقنا من صحة بنية YAML فقط (`yaml.safe_load` ناجح) ومن اتساق أسماء المتغيرات
    مع `.env.example`/`schema.prisma`، وليس من تشغيل `docker compose up` فعليًا
    حتى النهاية. يُنصَح بتشغيله فعليًا أول مرة على بيئة تملك Docker قبل اعتماده
    بالإنتاج.

## ملاحظة تشغيلية: حزمة `packages/shared`

هذه الحزمة (الأدوار، أنواع الجهات، تسميات عربية موحّدة) تُستهلك من `apps/web` و`apps/api`
معًا، وتحتاج بناءً صريحًا قبل التشغيل (`npm run build --workspace=packages/shared`) —
مُدرَج تلقائيًا عبر `postinstall` بجذر المشروع. عند تعديل أي ملف بها أثناء التطوير، شغّلي
البناء يدويًا مجددًا (أو `npm run dev --workspace=packages/shared` للبناء المستمر) قبل أن
تظهر التغييرات بالتطبيقين.

</div>
